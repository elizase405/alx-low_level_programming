# Usage
# make		# run the all target

CC = gcc
SRC = main.c school.c
OBJ = ${SRC:.c=.o}
NAME = school
RM = rm

all: re

clean:
	${RM} *.c~ ${NAME}

oclean:
	${RM} *.o

fclean:
	$(RM) *.c~ $(NAME) *.o

re: ${SRC}
	${CC} -c ${SRC} -o ${NAME}

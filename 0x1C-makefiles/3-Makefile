# Usage
# make		# run the all target

CC = gcc
SRC = main.c school.c
OBJ = ${SRC:.c=.o}
NAME = school
RM = rm

all: ${SRC}
	${CC} -c ${SRC} -o ${NAME}

clean:
	${RM} *~ ${NAME}

oclean:
	${RM} *.o

fclean:
	$(RM) *~ $(NAME) *.o

re: ${SRC}
	${RM} ${OBJ}
	${CC} -c ${SRC} -o ${NAME}

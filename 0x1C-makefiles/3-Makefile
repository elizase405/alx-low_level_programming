# Usage
# make		# run the all target

CC = gcc
SRC = main.c school.c
OBJ = ${SRC:.c=.o}
NAME = school
RM = rm -f

all: ${SRC}
	${CC} ${SRC} -o ${NAME}

clean:
	${RM} *~ ${NAME}

oclean:
	${RM} *.o

fclean: oclean clean

re: oclean all

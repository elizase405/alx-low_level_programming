# Usage
# make		# run the all target

CC = gcc
SRC = main.c school.c
OBJ = ${SRC:.c=.o}
NAME = school
RM = rm

all: ${SRC}
	${CC} -c ${SRC} -o ${NAME}

clean:
	${RM} -f *~ ${NAME}

oclean:
	${RM} -f *.o

fclean:
	$(RM) -f *~ $(NAME) *.o

re: ${SRC}
	${RM} -f ${OBJ}
	${CC} -c ${SRC} -o ${NAME}

#include <stdlib.h>
#include "main.h"

/**
 * _isupper - checks if character is uppercase
 * @c: character to be tested
 *
 * Return 1 or 0
 */

int _ispuuer(int c)
{
	if (isupper(c))
		return 1;
	return 0;
}

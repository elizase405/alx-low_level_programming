#include "main.h"

/**
 * print_sign - Entry point
 * Description: prints 1 if n > 0, and 0 if n = 0 and -1 if n < 0
 *
 * Return: 0 or 1 or -1
 */

int print_sign(int n)
{
	if (n > 0)
		return (1);
	else if (n = 0)
		return (0);
	else
		return -1;
}
